# ─────────────────────────────────────────────────────────────────────────
# SOCPilot — Environment Configuration
#
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit credentials.
#
# Private-subnet deployment: all MCP tool calls stay on your network.
# Only the AI inference request crosses the boundary.
# ─────────────────────────────────────────────────────────────────────────

# ── AI Inference ──────────────────────────────────────────────────────────
#
# OPTION A — Anthropic API (default, calls Anthropic externally)
#   AI_PROVIDER=anthropic
#   AI_API_KEY=sk-ant-api03-...          ← required
#   AI_MODEL=claude-opus-4-6             ← optional override
#
# OPTION B — Local LLM, fully air-gapped (Ollama / LM Studio / vLLM)
#   Install Ollama:  https://ollama.ai   then:  ollama pull qwen2.5:7b
#   AI_PROVIDER=openai
#   AI_BASE_URL=http://localhost:11434/v1   ← Ollama  (LM Studio: :1234, vLLM: :8080)
#   AI_MODEL=qwen2.5:7b                     ← or llama3.2:3b / mistral:7b / llama3.1:8b
#   AI_API_KEY=ollama                        ← any non-empty string (local servers ignore it)
#
AI_PROVIDER=anthropic
AI_API_KEY=
# AI_MODEL=
# AI_BASE_URL=

# ── Agent Mode ────────────────────────────────────────────────────────────
# mock  → realistic sample data, zero external MCP calls (default / CI)
# live  → routes to MCP servers listed in mcp_config.json
MCP_MODE=mock

# ─────────────────────────────────────────────────────────────────────────
# MCP Server Credentials
# Only needed when MCP_MODE=live.
# ─────────────────────────────────────────────────────────────────────────

# ── SIEM & Monitoring ─────────────────────────────────────────────────────
# Microsoft Sentinel
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=
SENTINEL_WORKSPACE_ID=
SENTINEL_RESOURCE_GROUP=
SENTINEL_WORKSPACE_NAME=

# Splunk
SPLUNK_URL=
SPLUNK_TOKEN=

# Elastic
ELASTICSEARCH_URL=
ELASTICSEARCH_API_KEY=

# Datadog
DD_API_KEY=
DD_APP_KEY=

# ── Vulnerability Scanning ────────────────────────────────────────────────
GITHUB_TOKEN=
GITHUB_OWNER=
SEMGREP_APP_TOKEN=
SNYK_TOKEN=
SNYK_ORG_ID=
STACKHAWK_API_KEY=

# ── Threat Intelligence ───────────────────────────────────────────────────
GREYNOISE_API_KEY=
OPENCTI_URL=
OPENCTI_TOKEN=
VT_API_KEY=

# ── Cloud Security ────────────────────────────────────────────────────────
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# ── Secrets & Identity ────────────────────────────────────────────────────
VAULT_ADDR=
VAULT_TOKEN=
HCP_CLIENT_ID=
HCP_CLIENT_SECRET=
HCP_PROJECT_ID=
OKTA_ORG_URL=
OKTA_API_TOKEN=

# ── Compliance ────────────────────────────────────────────────────────────
DRATA_API_KEY=
VANTA_API_TOKEN=

# ── Ticketing & Notifications ─────────────────────────────────────────────
JIRA_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
SLACK_BOT_TOKEN=
SLACK_TEAM_ID=